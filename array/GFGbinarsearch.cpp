#include<iostream>
using namespace std;

    int main()
    {
          int arr[],size,beg,end,mid,x;
    beg=0;
    end=size-1;

    cout<<"Enter The size of array : ";
    cin>>size;

    for (int i = 0; i < size; i++)
    {   cout<<"Enter The value of array : ";
        cin>>arr[i];
    }

    cout<<"Enter The finding number of array : ";
    cin>>x;
    
    while(x==a )

    
    return 0;
    }

// while (beg <= end)  
//     {  
//         /* get the mid value of sorted array and then compares with target element. */  
//         int mid = (beg + end) /2;         
//         if (x == arr[mid])  
//         {  
//             return mid; // when mid is equal to tgt value  
//         }         
//         // check tgt is less than mid value, discard left element  
//         else if (tgt < arr[mid])  
//         {  
//             end = mid - 1;  
//         }         
//         // if the target is greater than the mid value, discard all elements  
//         else {  
//             beg = mid + 1;  
//         }  
//     }  




